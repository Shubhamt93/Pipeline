pipeline  {
    agent any
    stages {
        stage ('Pull-Code')  {
            steps {
                sh 'printenv'
            }
        }
        stage ('Print-Env')  {
             step  {
                 echo "Build Id is ${env.BUILD_ID}"
                 echo "Node Label is ${NODE_LABELS}"
                 echo "Git Commit is "
             }   
        }
    }
}